# Global queue and pointer for q()
q1 = []
pointer = 0
val2 = 0

#This is my basic queue
def q(command, value=None):
    global q1, pointer
    # Ensure the queue always has 10 slots
    for x in range(len(q1), 10):
        q1.append(None)

    print("Current queue:", q1)
    function = command.lower()

    if function == "add":
        if pointer == 10:
            print("List full")
            val = input("Do you want to pop?")
            val = val.lower()
            if val == "yes":
                q1.pop(0)
                q1.append(value)
            elif val == "no":
                print("Okay pop some items when your ready!")
            else:
                print("No valed input")
        else:
            q1[pointer] = value
            pointer += 1
    elif function == "pop":
        if pointer == 0:
            print("Queue is empty")
        else:
            removed = q1.pop(0)
            q1.append(None)
            pointer -= 1
    else:
        print("No valid command")

    print("Queue now:", q1)
